# Optional — only needed for homonym_eval.py's extras.
# Stage A (rule/NLP divergence estimate) is stdlib-only and always runs; these
# packages only improve/extend it:
#   rapidfuzz  — better fuzzy string matching for Stage A (falls back to the
#                stdlib difflib if not installed; scores may differ slightly)
#   anthropic  — enables Stage B (--llm), the haiku adjudication call
#
#   pip install -r requirements-llm.txt
#   export ANTHROPIC_API_KEY=sk-ant-...
#   python homonym_eval.py out/placenames-ALL.tsv --llm
rapidfuzz>=3.0
anthropic>=0.69
